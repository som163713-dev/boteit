# اضافه‌شده: ماژول «کارخانه‌ی ایجنت» (Factory)

این قابلیت جدید، روی همون زیرساخت واقعی پروژه (همون Gemini، همون دیتابیس، همون
`api_key` مشتری‌ها) سوار شده — نه یه سرویس جدا. **هیچ فایل قدیمی‌ای پاک یا
بازنویسی نشده**، فقط این‌ها اضافه/ویرایش شدن:

## فایل‌های جدید
- `backend/factory_prompts.py` — پرامپت‌های تولید محتوا/کمپین/گزارش
- `backend/routes/factory.py` — اندپوینت‌های جدید

## فایل‌های ویرایش‌شده (فقط اضافه‌شده، چیزی حذف نشده)
- `backend/models.py` — جدول جدید `ContentItem` (برای ذخیره‌ی خروجی‌های واقعی روی دیتابیس، نه فایل محلی — چون دیسک Render بین ری‌استارت پایدار نیست)
- `backend/database.py` — یک import اضافه شد تا `init_db()` جدول جدید رو هم بسازه
- `backend/main.py` — روتر جدید `factory_router` رجیستر شد

## اندپوینت‌های جدید (همه با همون `api_key` واقعی مشتری کار می‌کنن)

```bash
# تولید محتوای واقعی
curl -X POST https://<دامنه-تو>/factory/content \
  -H "Content-Type: application/json" \
  -d '{"api_key": "eai_...", "topic": "تخفیف پاییزه"}'

# طرح کمپین — بر اساس آمار واقعی پیام‌های همون مشتری (نه عدد فرضی)
curl -X POST https://<دامنه-تو>/factory/campaign \
  -H "Content-Type: application/json" \
  -d '{"api_key": "eai_...", "budget_toman": 2000000, "goal": "افزایش سفارش"}'

# گزارش واقعی تعامل (۷ روز اخیر، از جدول Message خودِ همون مشتری)
curl "https://<دامنه-تو>/factory/report?api_key=eai_...&days=7"

# تاریخچه‌ی همه‌ی خروجی‌های قبلی این مشتری
curl "https://<دامنه-تو>/factory/history?api_key=eai_..."
```

## نکته‌ی مهم درباره‌ی «واقعی بودن» گزارش‌ها

این اپ در حال حاضر داده‌ی فروش/درآمدِ **کسب‌وکار مشتری** رو ذخیره نمی‌کنه — فقط
پیام‌های چت رو (جدول `Message`). پس `/factory/report` و `/factory/campaign` بر
اساس **حجم و دسته‌بندی واقعی پیام‌ها** گزارش می‌دن (رشد تعامل با ربات)، نه رشد
فروش واقعی. اگه بخوای گزارش رشد فروش واقعی هم داشته باشی، باید یه فیلد/جدول
جدید برای ثبت سفارش‌های واقعی مشتری اضافه کنی — می‌تونم اونم بسازم اگه لازمه.

## پنل ادمین

توی `admin-panel` یه تب جدید اضافه شده: **🏭 کارخانه‌ی ایجنت**. از اونجا:
۱. یه مشتری از لیست انتخاب کن (همون‌هایی که توی تب «مشتریان» هستن)
۲. روی «تولید محتوا»، «طرح کمپین» یا «گزارش واقعی» کلیک کن و جواب همون‌جا نمایش داده می‌شه
۳. پایین صفحه، تاریخچه‌ی همه‌ی خروجی‌های قبلی همون مشتری دیده می‌شه

فایل‌های ویرایش‌شده برای این بخش: `frontend/admin/index.html`، `frontend/admin/app.js`،
`frontend/admin/style.css` (فقط اضافه شده، چیزی از پنل قبلی حذف نشده).

## دیپلوی

هیچ dependency جدیدی لازم نیست (`requests` از قبل توی `requirements.txt` بود).
هیچ env var جدیدی هم لازم نیست — از همون `GEMINI_API_KEY` موجود استفاده می‌کنه.
فقط push کن؛ `init_db()` که توی `startup` اجرا می‌شه، جدول `content_items` رو
خودکار می‌سازه (بدون دست‌زدن به جدول‌های موجود).
